# Contributing

Use a focused branch and keep migrations forward-only. Before submitting changes, run formatting, lint, type checking, unit tests, database tests, and a production build. Never commit credentials, personal nutrition data, or generated local Supabase state. New private tables require RLS and policy tests in the same change.
